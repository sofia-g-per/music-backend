// export const SongDto = (data: Song): SongDto => {  
//     const { id, name, description } = data;

//     let SongDto: SongDto = { id, name, description, };
//     return SongDto;
    
// };
// export const toSongDto = (data: Song): SongDto => {  
//     const { id, name, description } = data;

//     let SongDto: SongDto = { id, name, description, };

//     return SongDto;
// };